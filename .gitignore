# Secrets — never commit (copy from .env.example)
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Build outputs (regenerate with `npm run build`)
dist/

# Electron: ignore all build output (DMGs distributed via GitHub Releases)
release/
out/

# Bridge (fetched by scripts/prepare-bridge.sh, not tracked in git)
bridge/

# Local dev certs (contains private key)
.certs/

# OS / editor
.DS_Store
*.log
.idea/
.vscode/
*.swp
*.swo

# Debug / local
*.local
coverage/
.vercel

# Local raw cover sources (bundled copies live in public/ and src/assets/)
CD_Slim.webp
cd-case.webp
